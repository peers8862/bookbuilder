[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bookbuilder"
version = "0.1.0"
author = "Your Name"
author-email = "your.email@example.com"
description = "A project for building and analyzing books"
license = "MIT"
